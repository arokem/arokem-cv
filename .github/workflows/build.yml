on:
    push:
      branches:
        - main
    pull_request:
      branches:
        - main

name: Check spelling

jobs:
    codespell:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: codespell-project/actions-codespell@master

